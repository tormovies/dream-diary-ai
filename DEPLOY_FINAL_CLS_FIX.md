# 🚀 Команды для деплоя финального исправления CLS

**Дата:** 2026-01-24  
**Коммит:** Финальное исправление CLS: увеличен min-height для main-grid до 1200px, главный контейнер до 1400px

---

## 📋 Команды для выполнения на сервере

### Быстрая команда (все сразу):

```bash
cd ~/snovidec.ru/laravel && \
git pull origin main && \
npm run build && \
php8.3 artisan view:clear && \
php8.3 artisan cache:clear && \
php8.3 artisan view:cache && \
echo "=== Проверка ===" && \
curl -s https://snovidec.ru/ | grep -o 'min-height:[^;]*' | head -5
```

---

## 📝 Что было изменено

### 1. Главный контейнер
- **Было:** `min-height: 1200px;`
- **Стало:** `min-height: 1400px;`
- **Файл:** `resources/views/welcome.blade.php`

### 2. main-grid в CSS
- **Было:** `min-height: 800px;`
- **Стало:** `min-height: 1200px;`
- **Файл:** `resources/css/app.css`

### 3. main-grid inline (обе версии)
- **Было:** `min-height: 1000px;`
- **Стало:** `min-height: 1200px;`
- **Файл:** `resources/views/welcome.blade.php`

---

## ✅ Проверка после деплоя

### 1. Проверить HTML:
```bash
curl -s https://snovidec.ru/ | grep -o 'min-height:[^;]*' | head -5
```

**Должны увидеть:**
- `min-height: 1400px` (главный контейнер)
- `min-height: 1200px` (main-grid, два раза)

### 2. Проверить через PageSpeed Insights:
- Открыть: https://pagespeed.web.dev/
- Проверить главную: `https://snovidec.ru/`
- **Ожидается:** CLS < 0.1 ✅

### 3. Проверить через Lighthouse:
- F12 → Маяк (Lighthouse)
- Запустить проверку Performance
- Проверить CLS - должен быть < 0.1 ✅

---

## 📊 Ожидаемые результаты

### До исправления:
- **CLS:** 0.233 🟠
- **Проблемный элемент:** `<div class="main-grid w-full" style="min-height: 1000px;">`

### После исправления:
- **CLS:** < 0.1 🟢 (ожидается значительное улучшение)
- **Причина:** Более агрессивное резервирование места для main-grid (1200px вместо 1000px)

---

## 🔍 Если CLS всё ещё высокий

### Дополнительная диагностика:

1. **Проверить, что изменения применены:**
   ```bash
   grep -n "min-height.*1200px" resources/views/welcome.blade.php
   ```

2. **Проверить CSS:**
   ```bash
   grep -n "min-height.*1200px" resources/css/app.css
   ```

3. **Проверить через Performance Panel в Chrome:**
   - F12 → Performance → Record → Reload
   - Найти красные блоки "Layout Shift"
   - Посмотреть, какие элементы вызывают shift

---

**Статус:** ✅ Готово к деплою
