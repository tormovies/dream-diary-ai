# 🔍 Руководство по проверке SEO улучшений

**Дата:** 2026-01-24

---

## ✅ Что нужно проверить

### 1. Aria-label для аватаров
### 2. Width/Height для изображений
### 3. Дополнительные OG мета-теги
### 4. Article мета-теги для статей

---

## 🔧 Способы проверки

### Способ 1: Проверка через браузер (DevTools)

#### 1.1. Проверка aria-label для аватаров

1. Откройте любую страницу с аватарами (например, `/dashboard`, `/users`, `/activity`)
2. Откройте DevTools (F12)
3. Найдите элемент аватара (можно через поиск по классу `rounded-full`)
4. Проверьте в инспекторе элементов:
   - Должен быть атрибут `role="img"`
   - Должен быть атрибут `aria-label="Аватар пользователя {nickname}"`

**Пример:**
```html
<div class="w-12 h-12 rounded-full ..." role="img" aria-label="Аватар пользователя Иван">
  <span>И</span>
</div>
```

#### 1.2. Проверка width/height для изображений

1. Откройте страницу с изображениями (если используются компонент `lazy-image`)
2. Откройте DevTools (F12)
3. Найдите тег `<img>` в инспекторе
4. Проверьте наличие атрибутов `width` и `height` (если они были переданы)

**Пример:**
```html
<img src="..." alt="..." width="800" height="600" loading="lazy" />
```

#### 1.3. Проверка OG мета-тегов

1. Откройте любую страницу (например, главную `/` или статью `/articles/{slug}`)
2. Откройте DevTools (F12)
3. Перейдите на вкладку **Elements** (Элементы)
4. Найдите тег `<head>` и разверните его
5. Проверьте наличие следующих мета-тегов:

**Для всех страниц:**
```html
<meta property="og:locale" content="ru_RU">
<meta property="og:type" content="website">
<meta property="og:site_name" content="Дневник сновидений">
```

**Для статей и инструкций:**
```html
<meta property="og:type" content="article">
<meta property="article:author" content="Дневник сновидений">
<meta property="article:published_time" content="2026-01-24T12:00:00+00:00">
<meta property="article:modified_time" content="2026-01-24T12:00:00+00:00">
```

**Если есть OG изображение:**
```html
<meta property="og:image" content="https://snovidec.ru/...">
<meta property="og:image:width" content="1200">
<meta property="og:image:height" content="630">
```

---

### Способ 2: Проверка через View Source (Просмотр кода страницы)

1. Откройте страницу в браузере
2. Нажмите `Ctrl+U` (или ПКМ → "Просмотр кода страницы")
3. Найдите нужные элементы через поиск (`Ctrl+F`)

**Что искать:**
- `aria-label="Аватар пользователя` - для аватаров
- `og:locale` - для локали
- `article:author` - для статей
- `width="` и `height="` - для изображений

---

### Способ 3: Онлайн инструменты

#### 3.1. Facebook Sharing Debugger
**URL:** https://developers.facebook.com/tools/debug/

**Как использовать:**
1. Вставьте URL страницы (например, `https://snovidec.ru/articles/slug`)
2. Нажмите "Debug"
3. Проверьте:
   - `og:locale` должен быть `ru_RU`
   - `og:type` должен быть `article` для статей
   - `article:author`, `article:published_time` должны присутствовать

#### 3.2. Twitter Card Validator
**URL:** https://cards-dev.twitter.com/validator

**Как использовать:**
1. Вставьте URL страницы
2. Проверьте отображение карточки
3. Убедитесь, что все мета-теги корректны

#### 3.3. Schema.org Validator
**URL:** https://validator.schema.org/

**Как использовать:**
1. Вставьте URL страницы или HTML код
2. Проверьте структурированные данные
3. Убедитесь, что нет ошибок

#### 3.4. Google Rich Results Test
**URL:** https://search.google.com/test/rich-results

**Как использовать:**
1. Вставьте URL страницы
2. Проверьте наличие Article разметки
3. Убедитесь, что все поля заполнены

---

### Способ 4: Проверка через Artisan команду

#### 4.1. Использование существующей команды `seo:check`

```bash
cd C:\Users\torle\snovidec
php artisan seo:check --url=http://127.0.0.1:3000
```

**Что проверяет:**
- Title, Description, H1
- Canonical URLs
- Open Graph теги
- JSON-LD структурированные данные

**Результат:** CSV файл `seo_check.csv` с результатами проверки

---

### Способ 5: Ручная проверка через cURL

#### 5.1. Получить HTML страницы

```bash
curl http://127.0.0.1:3000/articles/slug > page.html
```

#### 5.2. Проверить наличие мета-тегов

```bash
# Windows PowerShell
Select-String -Path page.html -Pattern "og:locale"
Select-String -Path page.html -Pattern "article:author"
Select-String -Path page.html -Pattern "aria-label.*Аватар"
```

---

## 📋 Чеклист проверки

### ✅ Aria-label для аватаров
- [ ] Открыть страницу с аватарами (`/dashboard`, `/users`, `/activity`)
- [ ] Проверить в DevTools наличие `aria-label` и `role="img"`
- [ ] Проверить, что текст содержит "Аватар пользователя {nickname}"

### ✅ Width/Height для изображений
- [ ] Найти страницы, где используется `<x-lazy-image>`
- [ ] Проверить, что при передаче `width` и `height` они добавляются в `<img>`
- [ ] Убедиться, что атрибуты корректны

### ✅ OG мета-теги
- [ ] Проверить главную страницу (`/`) - должен быть `og:locale="ru_RU"`
- [ ] Проверить статью (`/articles/{slug}`) - должен быть `og:type="article"`
- [ ] Проверить инструкцию (`/guide/{slug}`) - должен быть `og:type="article"`
- [ ] Проверить наличие `article:author`, `article:published_time`, `article:modified_time` для статей

### ✅ Article мета-теги
- [ ] Открыть любую статью (`/articles/{slug}`)
- [ ] Проверить в `<head>` наличие:
  - `og:type="article"`
  - `article:author="Дневник сновидений"`
  - `article:published_time` (дата в ISO8601)
  - `article:modified_time` (дата в ISO8601)

---

## 🐛 Возможные проблемы и решения

### Проблема 1: Article мета-теги не появляются

**Причина:** Модель `$article` не передается в `SeoHelper::get()`

**Решение:** Убедитесь, что в `ArticleController.php` передается:
```php
$seo = SeoHelper::get('article', $article->id, [
    'title' => $article->title,
    'slug' => $article->slug,
    'article' => $article, // ← Важно!
]);
```

### Проблема 2: Aria-label не отображается

**Причина:** Компонент `avatar.blade.php` не обновлен

**Решение:** Проверьте, что в файле есть:
```php
$ariaLabel = "Аватар пользователя {$userName}";
```
И в `<div>` есть:
```html
role="img" aria-label="{{ $ariaLabel }}"
```

### Проблема 3: Width/Height не работают

**Причина:** Параметры не передаются в компонент

**Решение:** Убедитесь, что при использовании компонента передаются параметры:
```blade
<x-lazy-image src="..." alt="..." width="800" height="600" />
```

---

## 🔍 Быстрая проверка (5 минут)

1. **Откройте главную страницу** → Проверьте `og:locale="ru_RU"` в `<head>`
2. **Откройте любую статью** → Проверьте `og:type="article"` и `article:author`
3. **Откройте страницу с аватарами** → Проверьте `aria-label` в DevTools
4. **Запустите `php artisan seo:check`** → Проверьте CSV отчет

---

## 📊 Ожидаемые результаты

### Для главной страницы:
- ✅ `og:locale="ru_RU"`
- ✅ `og:type="website"`
- ✅ `og:site_name="Дневник сновидений"`

### Для статей:
- ✅ `og:type="article"`
- ✅ `article:author="Дневник сновидений"`
- ✅ `article:published_time` (ISO8601 формат)
- ✅ `article:modified_time` (ISO8601 формат)

### Для аватаров:
- ✅ `role="img"`
- ✅ `aria-label="Аватар пользователя {nickname}"`

---

## 🚀 После проверки

Если все проверки пройдены успешно:
1. ✅ Изменения работают корректно
2. ✅ Можно деплоить на продакшен
3. ✅ SEO оптимизация улучшена

Если есть проблемы:
1. ⚠️ Проверьте логи (`storage/logs/laravel.log`)
2. ⚠️ Убедитесь, что все файлы сохранены
3. ⚠️ Очистите кэш: `php artisan view:clear && php artisan config:clear`

---

**Дата создания:** 2026-01-24
