# Сновидец.ру - Дневник сновидений

Веб-приложение для ведения личных дневников сновидений с возможностью анализа снов через AI на Laravel.

## 📋 Описание

**Сновидец.ру** — это платформа для записи, анализа и интерпретации сновидений. Пользователи могут вести личный дневник снов, делиться записями с сообществом, получать глубокий психологический анализ снов через интеграцию с AI (DeepSeek API), искать паттерны в своих снах, общаться с единомышленниками и многое другое.

## ✨ Основной функционал

### Для пользователей:
- ✅ Регистрация с подтверждением email
- ✅ Создание отчетов о снах с динамическим добавлением снов
- ✅ Система тегов с автодополнением
- ✅ Настройки приватности дневника (публичный/приватный/только друзья)
- ✅ Настройки доступа к отчетам (всем/никому/друзьям)
- ✅ Публичные ссылки на дневники
- ✅ Система друзей (запросы, принятие, отклонение)
- ✅ Комментарии к отчетам с вложенностью
- ✅ Поиск и фильтрация по тексту, тегам, дате, типам снов
- ✅ Лента активности
- ✅ Личная статистика по снам
- ✅ Профили пользователей с аватарами
- ✅ Переключение светлой/темной темы
- ✅ Система уведомлений
- ✅ **AI-анализатор снов** с использованием различных традиций интерпретации:
  - Фрейдистская
  - Юнгианская
  - Когнитивная
  - Символическая
  - Шаманистическая
  - Гештальт
  - Комплексный анализ
- ✅ Анализ как одиночных снов, так и серий снов
- ✅ Детальная расшифровка с ключевыми символами, локациями, тегами и рекомендациями

### Для администраторов:
- ✅ Полнофункциональная админ-панель
- ✅ Управление пользователями (просмотр, редактирование)
- ✅ Управление дневниками и отчетами
- ✅ Управление комментариями
- ✅ Настройки системы (ограничения на редактирование/удаление)
- ✅ Управление SEO мета-тегами

## 🛠 Технологический стек

- **Backend:** Laravel 12 (PHP 8.2+)
- **Database:** SQLite (по умолчанию), MySQL/MariaDB (поддерживается)
- **Frontend:** 
  - Tailwind CSS (с поддержкой темной темы)
  - Alpine.js (для интерактивности)
  - Vite (сборка фронтенда)
- **Authentication:** Laravel Breeze
- **AI Integration:** DeepSeek API (для анализа снов)

## 📦 Требования

- PHP >= 8.2 (рекомендуется 8.3+)
- Composer
- Node.js >= 18 и npm
- SQLite (встроен в PHP) или MySQL/MariaDB

## 🚀 Установка

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/ваш-username/название-репозитория.git
   cd название-репозитория
   ```

2. **Установите зависимости PHP:**
   ```bash
   composer install
   ```

3. **Установите зависимости Node.js:**
   ```bash
   npm install
   ```

4. **Скопируйте файл окружения:**
   ```bash
   copy .env.example .env
   ```

5. **Создайте файл базы данных SQLite (если используете SQLite):**
   ```bash
   touch database/database.sqlite
   ```

   Или настройте `.env` для использования MySQL/MariaDB:
   ```env
   DB_CONNECTION=mysql
   DB_HOST=127.0.0.1
   DB_PORT=3306
   DB_DATABASE=your_database
   DB_USERNAME=your_username
   DB_PASSWORD=your_password
   ```

6. **Сгенерируйте ключ приложения:**
   ```bash
   php artisan key:generate
   ```

7. **Выполните миграции:**
   ```bash
   php artisan migrate
   ```

8. **Создайте администратора (опционально):**
   ```bash
   php artisan app:create-admin
   ```

9. **Соберите фронтенд:**
   ```bash
   npm run build
   ```

   Или для разработки с hot-reload:
   ```bash
   npm run dev
   ```

10. **Настройте API ключ для анализатора снов (опционально):**
   
    В админ-панели или через базу данных добавьте настройку `deepseek_api_key` с вашим API ключом DeepSeek.

## ⚙️ Конфигурация

### Настройка базы данных

По умолчанию проект использует SQLite. Для изменения на MySQL/MariaDB отредактируйте `.env`:

```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=your_database
DB_USERNAME=your_username
DB_PASSWORD=your_password
```

### Настройка анализатора снов

Для использования AI-анализатора снов необходим API ключ DeepSeek. Добавьте его через админ-панель в настройках системы (`deepseek_api_key`).

## 🌐 Запуск сервера

### Для разработки (PHP встроенный сервер):

```bash
php -S 127.0.0.1:3000 -t public public/server.php
```

Приложение будет доступно по адресу: http://127.0.0.1:3000

**Важно:** Используется `server.php` как роутер для правильной обработки статических файлов и маршрутов Laravel.

### Для production:

Настройте веб-сервер (Apache/Nginx) на директорию `public/` и следуйте [официальной документации Laravel](https://laravel.com/docs/deployment).

## 📁 Структура проекта

```
snovidec/
├── app/
│   ├── Console/Commands/      # Artisan команды
│   ├── Helpers/               # Хелперы (AvatarHelper, SeoHelper)
│   ├── Http/Controllers/      # Контроллеры
│   ├── Models/                # Eloquent модели
│   ├── Policies/              # Политики доступа
│   ├── Providers/             # Сервис-провайдеры
│   ├── Services/              # Сервисы (DeepSeekService)
│   └── View/Components/       # Blade компоненты
├── config/                    # Конфигурационные файлы
├── database/
│   ├── migrations/            # Миграции базы данных
│   └── seeders/               # Сидеры
├── docs/                      # Документация
├── public/                    # Публичная директория
├── resources/
│   ├── views/                 # Blade шаблоны
│   ├── css/                   # CSS файлы
│   └── js/                    # JavaScript файлы
├── routes/                    # Маршруты
└── storage/                   # Файлы, логи, кэш
```

## 🔑 Основные маршруты

- `/` - Главная страница
- `/tolkovanie-snov` - AI-анализатор снов
- `/activity` - Лента активности
- `/dashboard` - Мои отчеты
- `/statistics` - Статистика
- `/users` - Сообщество пользователей
- `/notifications` - Уведомления
- `/admin` - Админ-панель (только для администраторов)

## 🧪 Тестирование

```bash
php artisan test
```

## 📝 Документация

Дополнительная документация находится в директории `docs/`:
- `DREAM_ANALYSIS_STRUCTURE.md` - Структура данных для анализа одиночных снов
- `SERIES_ANALYSIS_STRUCTURE.md` - Структура данных для анализа серий снов

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Запушьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Проект использует лицензию MIT. См. файл `LICENSE` для деталей.

## 👥 Авторы

- Ваше имя/команда

## 🙏 Благодарности

- Laravel Framework
- DeepSeek API
- Tailwind CSS
- Alpine.js
- Всех контрибьюторов проекта

---

**Примечание:** Проект находится в активной разработке. Некоторые функции могут изменяться.

























