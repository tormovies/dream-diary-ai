# Управление пользователями в админ-панели

## Обзор

Администраторы имеют возможность управлять пользователями через админ-панель: блокировать/разблокировать аккаунты и удалять пользователей вместе со всем их контентом.

## Функционал

### 1. Блокировка пользователей

**Путь:** `/admin/users` → кнопка "Заблокировать"

**Возможности:**
- Блокировка пользователя с указанием причины
- **Автоматическая установка `diary_privacy = 'private'`** - весь контент становится недоступен
- Скрываются: дневник, отчеты, анализы снов, комментарии (видны только админам)
- Заблокированный пользователь автоматически выходит из системы при следующем запросе
- Заблокированный пользователь не может войти в систему
- При попытке входа показывается причина блокировки
- Предыдущая настройка приватности сохраняется для восстановления

**Ограничения:**
- Нельзя заблокировать самого себя
- Нельзя заблокировать другого администратора

**Технические детали:**
- Поля в БД: `is_banned` (boolean), `banned_at` (timestamp), `ban_reason` (text)
- При блокировке: `diary_privacy` → `'private'` (автоматически)
- Старое значение `diary_privacy` сохраняется в `ban_reason` через разделитель `|`
- Middleware `CheckBanned` проверяет статус при каждом запросе
- Метод контроллера: `AdminController@banUser`
- Маршрут: `POST /admin/users/{user}/ban`

### 2. Разблокировка пользователей

**Путь:** `/admin/users` → кнопка "Разблокировать"

**Возможности:**
- Снятие блокировки с пользователя
- **Автоматическое восстановление предыдущей настройки `diary_privacy`**
- Если предыдущее значение не сохранено - устанавливается `'public'`
- Очистка причины блокировки
- Пользователь может снова войти в систему
- Контент становится доступен согласно восстановленной настройке приватности

**Технические детали:**
- Метод контроллера: `AdminController@unbanUser`
- Маршрут: `POST /admin/users/{user}/unban`
- Восстанавливает `diary_privacy` из сохраненного значения в `ban_reason`

### 3. Удаление пользователей

**Путь:** `/admin/users` → кнопка "Удалить"

**Возможности:**
- Полное удаление пользователя из системы
- Каскадное удаление всего контента:
  - **Анализы снов (dream_interpretations)**
    - Результаты анализов (dream_interpretation_results)
    - Детали снов в сериях (dream_interpretation_series_dreams)
  - **Отчеты о снах (reports)**
    - Сны в отчетах (dreams)
    - Связи с тегами (report_tag)
    - Комментарии к отчетам
  - Комментарии пользователя (comments)
  - Дружеские связи (friendships) - в обе стороны
  - Уведомления (notifications)

**Ограничения:**
- Нельзя удалить самого себя
- Нельзя удалить другого администратора
- Требуется подтверждение действия
- **Действие необратимо!**

**Технические детали:**
- Метод контроллера: `AdminController@deleteUser`
- Маршрут: `DELETE /admin/users/{user}`
- Удаление происходит последовательно с проверкой всех связей

## Интерфейс

### Список пользователей

В таблице пользователей добавлена колонка "Статус":
- **Активен** (зеленый бейдж) - пользователь может работать с системой
- **Заблокирован** (красный бейдж) - пользователь заблокирован, при наведении показывается причина

Заблокированные пользователи выделяются красным фоном строки.

### Действия

Для каждого пользователя (кроме администраторов и самого себя) доступны:
- **Редактировать** - переход к редактированию профиля
- **Профиль** - просмотр публичного профиля
- **Заблокировать** - открывает модальное окно с полем для причины блокировки
- **Разблокировать** - снимает блокировку (показывается только для заблокированных)
- **Удалить** - удаляет пользователя с подтверждением

## Модальное окно блокировки

При нажатии "Заблокировать" открывается модальное окно с:
- Заголовком "Блокировка пользователя"
- Текстовым полем для указания причины (необязательно)
- Кнопками "Отмена" и "Заблокировать"

## Модель User

Добавлены методы:
```php
// Проверка блокировки
$user->isBanned(): bool

// Блокировка с причиной
// Автоматически устанавливает diary_privacy = 'private'
// Сохраняет предыдущее значение diary_privacy в ban_reason
$user->ban(?string $reason = null): void

// Разблокировка
// Восстанавливает предыдущее значение diary_privacy
$user->unban(): void
```

Добавлена связь:
```php
// Анализы снов пользователя
$user->dreamInterpretations(): HasMany
```

## Проверка блокировки

### При входе в систему (LoginRequest)

При попытке входа заблокированного пользователя:
- Проверка выполняется после успешной аутентификации по email/паролю
- Пользователь автоматически выходит из системы
- Показывается сообщение с причиной блокировки прямо на форме входа
- Сообщение отображается в поле email как ошибка валидации

### Во время работы (Middleware CheckBanned)

Автоматически проверяет статус пользователя при каждом запросе:
- Если пользователь заблокирован - выполняется logout
- Редирект на страницу входа с сообщением о блокировке и причиной

Middleware применяется ко всей группе `web` маршрутов.

## Миграция

Файл: `2025_12_31_152737_add_is_banned_to_users_table.php`

Добавленные поля:
- `is_banned` - boolean, default: false
- `banned_at` - timestamp, nullable
- `ban_reason` - text, nullable

## Маршруты

```php
// Блокировка
POST /admin/users/{user}/ban

// Разблокировка
POST /admin/users/{user}/unban

// Удаление
DELETE /admin/users/{user}
```

## Безопасность

1. Все действия доступны только администраторам (middleware `admin`)
2. Защита от блокировки/удаления самого себя
3. Защита от блокировки/удаления других администраторов
4. Подтверждение удаления через JavaScript confirm
5. CSRF защита через `@csrf` токены

## Уведомления

После каждого действия показывается flash-сообщение:
- Успех: зеленое сообщение с подтверждением действия
- Ошибка: красное сообщение с описанием проблемы

## Темная тема

Интерфейс полностью поддерживает темную тему через Tailwind CSS классы `dark:`.

---

## 📋 Краткая инструкция

### Что происходит при блокировке:
1. ✅ Пользователь не может войти (видит причину блокировки)
2. ✅ `diary_privacy` автоматически → `'private'`
3. ✅ Весь контент скрыт от всех (кроме админов)
4. ✅ Старое значение приватности сохраняется

### Что происходит при разблокировке:
1. ✅ Пользователь может войти
2. ✅ `diary_privacy` восстанавливается (или `'public'` по умолчанию)
3. ✅ Контент снова доступен согласно настройкам

### Что происходит при удалении:
1. ❌ Пользователь удален навсегда
2. ❌ Все анализы снов удалены
3. ❌ Все отчеты и сны удалены
4. ❌ Все комментарии удалены
5. ❌ Все дружеские связи удалены
6. ❌ Все уведомления удалены
7. ⚠️ **Действие необратимо!**

### Защита:
- ⛔ Нельзя заблокировать/удалить себя
- ⛔ Нельзя заблокировать/удалить другого админа
- ✅ Требуется подтверждение при удалении

