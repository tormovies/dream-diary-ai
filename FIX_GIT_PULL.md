# 🔧 Исправление проблемы с git pull

**Проблема:** Git не может выполнить pull из-за локальных изменений в `public/build/manifest.json`

**Причина:** Файл `public/build/manifest.json` генерируется автоматически при `npm run build` и отличается на сервере и локально.

---

## 📋 Решение: Вариант 1 (Рекомендуемый)

### Сохранить локальные изменения и обновить:

```bash
cd ~/snovidec.ru/laravel

# Сохранить локальные изменения во временное хранилище
git stash

# Получить последние изменения
git pull origin main

# Пересобрать фронтенд (это обновит manifest.json)
npm run build

# Очистить кеш
php8.3 artisan view:clear
php8.3 artisan cache:clear
php8.3 artisan view:cache

# Проверить результат
curl -s https://snovidec.ru/ | grep -i "min-height.*1200px"
```

---

## 📋 Решение: Вариант 2 (Быстрый)

### Откатить изменения в manifest.json и обновить:

```bash
cd ~/snovidec.ru/laravel

# Откатить изменения в manifest.json
git checkout -- public/build/manifest.json

# Получить последние изменения
git pull origin main

# Пересобрать фронтенд
npm run build

# Очистить кеш
php8.3 artisan view:clear
php8.3 artisan cache:clear
php8.3 artisan view:cache

# Проверить результат
curl -s https://snovidec.ru/ | grep -i "min-height.*1200px"
```

---

## 📋 Решение: Вариант 3 (Принудительный)

### Принудительно обновить до последней версии:

```bash
cd ~/snovidec.ru/laravel

# Принудительно обновить до последней версии
git reset --hard origin/main

# Пересобрать фронтенд
npm run build

# Очистить кеш
php8.3 artisan view:clear
php8.3 artisan cache:clear
php8.3 artisan view:cache

# Проверить результат
curl -s https://snovidec.ru/ | grep -i "min-height.*1200px"
```

---

## 🎯 Рекомендуемая команда (Вариант 1)

```bash
cd ~/snovidec.ru/laravel && \
git stash && \
git pull origin main && \
npm run build && \
php8.3 artisan view:clear && \
php8.3 artisan cache:clear && \
php8.3 artisan view:cache && \
echo "=== Проверка ===" && \
curl -s https://snovidec.ru/ | grep -i "min-height.*1200px"
```

---

## ✅ Проверка после выполнения

### 1. Проверить, что файл обновился:
```bash
grep -n "min-height.*1200px" resources/views/welcome.blade.php
```

**Должны увидеть:** строку с `min-height: 1200px`

### 2. Проверить последний коммит:
```bash
git log --oneline -3
```

**Должен быть:** `c032197 Агрессивное исправление CLS...`

### 3. Проверить HTML:
```bash
curl -s https://snovidec.ru/ | grep -o 'min-height:[^;]*' | head -3
```

**Должны увидеть:** `min-height: 1200px`

---

## 🔍 Почему это происходит?

Файл `public/build/manifest.json` генерируется автоматически при сборке фронтенда (`npm run build`). Он содержит хеши файлов CSS/JS и может отличаться на разных машинах, даже если исходный код одинаковый.

**Решение:** Всегда пересобирать фронтенд после `git pull`, чтобы обновить этот файл.

---

**Статус:** ⚠️ Требуется выполнить одну из команд выше
