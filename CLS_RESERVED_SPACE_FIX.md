# 🔧 Исправление CLS через резервирование места

**Проблема:** CLS всё ещё 0.235, проблемный элемент - `<div class="main-grid w-full" style="min-height: 1200px;">`

**Причина:** main-grid появляется условно (@auth/@else), что вызывает layout shift при его появлении.

---

## ✅ Новый подход: Резервирование места через невидимый элемент

### Идея:
Вместо того чтобы main-grid появлялся условно, мы резервируем место для него заранее через невидимый элемент, который всегда присутствует в DOM.

### Изменения:

1. **Добавлен невидимый элемент для резервирования места:**
   ```html
   <div class="main-grid-reserved-space" style="min-height: 1200px; position: absolute; width: 100%; visibility: hidden; pointer-events: none; z-index: -1;" aria-hidden="true"></div>
   ```

2. **Добавлен класс main-content-container:**
   - Контейнер теперь имеет `position: relative;` для позиционирования невидимого элемента

3. **CSS для резервирования места:**
   - `.main-grid-reserved-space` - невидимый элемент, который резервирует место

---

## 📊 Ожидаемый результат

После деплоя:
- **CLS:** должен быть < 0.1 (было 0.235) ✅
- **Причина:** Место для main-grid резервируется заранее, до того как он появится

---

## 🚀 Команды для деплоя

```bash
cd ~/snovidec.ru/laravel && \
git pull origin main && \
npm run build && \
php8.3 artisan view:clear && \
php8.3 artisan cache:clear && \
php8.3 artisan view:cache && \
echo "=== Проверка ===" && \
curl -s https://snovidec.ru/ | grep -i "main-grid-reserved-space"
```

---

## ✅ Проверка после деплоя

### 1. Проверить HTML:
```bash
curl -s https://snovidec.ru/ | grep -i "main-grid-reserved-space"
```

**Должны увидеть:** `<div class="main-grid-reserved-space"...`

### 2. Проверить через PageSpeed Insights:
- Открыть: https://pagespeed.web.dev/
- Проверить главную: `https://snovidec.ru/`
- **Ожидается:** CLS < 0.1 ✅

---

**Статус:** ✅ Готово к коммиту и деплою
