# 🚀 СРОЧНЫЙ ДЕПЛОЙ: Получить последние изменения

**Проблема:** На сервере старый коммит, новые изменения с исправлениями CLS не задеплоены

**Текущий коммит на сервере:** `1b260ba` (старый)
**Нужный коммит:** `c032197` (Агрессивное исправление CLS)

---

## 📋 Команды для выполнения на сервере

### 1. Получить последние изменения из репозитория

```bash
cd ~/snovidec.ru/laravel
git pull origin main
```

**Ожидаемый результат:** Должны увидеть коммиты:
- "Исправление CLS 0.233: добавлено резервирование места..."
- "Агрессивное исправление CLS: увеличен min-height до 1200px..."

---

### 2. Проверить, что файлы обновились

```bash
grep -n "min-height.*1200px" resources/views/welcome.blade.php
```

**Должны увидеть:** строку с `min-height: 1200px`

---

### 3. Пересобрать фронтенд (КРИТИЧЕСКИ ВАЖНО!)

```bash
npm install
npm run build
```

**Проверка после сборки:**
```bash
ls -la public/build/assets/app-*.css | tail -1
```

Должен быть файл: `app-COn3ZSwi.css` (или новее)

---

### 4. Очистить кеш Laravel

```bash
php8.3 artisan view:clear
php8.3 artisan cache:clear
php8.3 artisan config:clear
php8.3 artisan route:clear
```

---

### 5. Пересобрать кеш views (ВАЖНО!)

```bash
php8.3 artisan view:cache
```

**Почему важно:** Laravel кеширует скомпилированные Blade шаблоны, поэтому после изменения `welcome.blade.php` нужно пересобрать кеш views.

---

### 6. Проверить результат

```bash
curl -s https://snovidec.ru/ | grep -i "min-height.*1200px"
```

**Должны увидеть:** `style="min-height: 1200px; contain: layout;"`

---

## 🎯 Быстрая команда (все сразу)

```bash
cd ~/snovidec.ru/laravel && \
git pull origin main && \
npm run build && \
php8.3 artisan view:clear && \
php8.3 artisan cache:clear && \
php8.3 artisan config:clear && \
php8.3 artisan view:cache && \
echo "=== Проверка ===" && \
curl -s https://snovidec.ru/ | grep -i "min-height.*1200px"
```

---

## ✅ Ожидаемый результат

После выполнения команд:

1. **Git log должен показать новые коммиты:**
   ```bash
   git log --oneline -3
   ```
   Должен быть: `c032197 Агрессивное исправление CLS...`

2. **HTML должен содержать min-height: 1200px:**
   ```bash
   curl -s https://snovidec.ru/ | grep -o 'min-height:[^;]*' | head -3
   ```
   Должны увидеть: `min-height: 1200px`

3. **CSS файл должен быть новым:**
   ```bash
   ls -la public/build/assets/app-*.css | tail -1
   ```
   Должен быть: `app-COn3ZSwi.css`

---

## 🔍 Если что-то пошло не так

### Проверить статус git:
```bash
git status
```

### Проверить удалённый репозиторий:
```bash
git fetch origin
git log origin/main --oneline -5
```

### Если нужно принудительно обновить:
```bash
git reset --hard origin/main
npm run build
php8.3 artisan view:clear
php8.3 artisan view:cache
```

---

**Статус:** ⚠️ Требуется выполнить `git pull` на сервере
