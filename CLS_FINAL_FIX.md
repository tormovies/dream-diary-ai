# 🔧 Финальное исправление CLS 0.233

**Проблема:** CLS всё ещё 0.233, проблемный элемент - `<div class="main-grid w-full" style="min-height: 1000px;">`

**Причина:** main-grid появляется условно (@auth/@else) и вызывает layout shift при появлении.

---

## ✅ Применённые исправления

### 1. Увеличен min-height на главном контейнере
- **Было:** `min-height: 1200px;`
- **Стало:** `min-height: 1400px;`
- **Файл:** `resources/views/welcome.blade.php` (строка 5)

### 2. Увеличен min-height на main-grid в CSS
- **Было:** `min-height: 800px;`
- **Стало:** `min-height: 1200px;`
- **Файл:** `resources/css/app.css` (строка 51)

### 3. Увеличен min-height на main-grid inline
- **Было:** `min-height: 1000px;` (для обеих версий)
- **Стало:** `min-height: 1200px;` (для обеих версий)
- **Файл:** `resources/views/welcome.blade.php` (строки 8, 274)

---

## 📊 Ожидаемый результат

После деплоя:
- **CLS:** должен быть < 0.1 (было 0.233) ✅
- **Причина:** Более агрессивное резервирование места для main-grid

---

## 🚀 Команды для деплоя

```bash
cd ~/snovidec.ru/laravel && \
git pull origin main && \
npm run build && \
php8.3 artisan view:clear && \
php8.3 artisan cache:clear && \
php8.3 artisan view:cache && \
echo "=== Проверка ===" && \
curl -s https://snovidec.ru/ | grep -i "min-height.*1200px" | head -3
```

---

## ✅ Проверка после деплоя

### 1. Проверить HTML:
```bash
curl -s https://snovidec.ru/ | grep -o 'min-height:[^;]*' | head -5
```

**Должны увидеть:**
- `min-height: 1400px` (главный контейнер)
- `min-height: 1200px` (main-grid)

### 2. Проверить через PageSpeed Insights:
- Открыть: https://pagespeed.web.dev/
- Проверить главную: `https://snovidec.ru/`
- **Ожидается:** CLS < 0.1 ✅

---

**Статус:** ✅ Готово к коммиту и деплою
