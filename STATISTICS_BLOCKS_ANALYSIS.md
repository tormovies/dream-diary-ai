# 📊 Анализ блоков статистики в проекте

**Дата:** 2026-01-24

---

## 📋 Типы блоков статистики

### 1. **"Наша статистика"** (кастомный блок)
- **Формат:** Grid 2x2 с карточками
- **Содержимое:** Всего снов, Отчетов, Пользователей, Снов/отчет
- **Иконка:** `fas fa-chart-line`
- **Расположение:** Правая колонка (для авторизованных)

### 2. **"Статистика проекта"** (компонент `<x-project-statistics>`)
- **Формат:** 
  - `variant="list"` - вертикальный список
  - `variant="grid"` - сетка 2x2
- **Содержимое:** Пользователей, Отчетов, Снов, Толкований
- **Иконка:** `fas fa-chart-bar`
- **Расположение:** Левая или правая колонка (в зависимости от страницы)

---

## 📍 Расположение блоков по страницам

### 1. **Главная страница** (`/`) - `welcome.blade.php`

#### Для авторизованных пользователей:

**Левая колонка:**
- Приветственная карточка
- Карточка пользователя
- Быстрое меню (`<x-auth-sidebar-menu />`)

**Правая колонка:**
- ✅ **"Наша статистика"** (кастомный блок, grid 2x2)
  - Всего снов
  - Отчетов
  - Пользователей
  - Снов/отчет
- Последние толкования (если есть)
- ✅ **"Статистика проекта"** (`<x-project-statistics variant="list" />`)
  - Пользователей
  - Отчетов
  - Снов
  - Толкований
- Популярные теги (если есть)

#### Для неавторизованных пользователей:

**Левая колонка:**
- Приветственная карточка
- ✅ **"Статистика проекта"** (`<x-project-statistics variant="list" />`)
- Быстрые действия (`<x-guest-quick-actions />`)

**Правая колонка:**
- ✅ **"Статистика проекта"** (`<x-project-statistics variant="list" />`)
- Популярные теги (если есть)

---

### 2. **Лента активности** (`/activity`) - `activity/index.blade.php`

#### Для авторизованных пользователей:

**Правая колонка:**
- ✅ **"Наша статистика"** (кастомный блок, grid 2x2)
  - Всего снов
  - Отчетов
  - Пользователей
  - Снов/отчет
- Друзья онлайн (если есть)
- Сонник дня
- Популярные теги (если есть)

#### Для неавторизованных пользователей:

**Левая колонка:**
- Приветственная карточка
- ✅ **"Статистика проекта"** (`<x-project-statistics variant="list" />`)
- Быстрые действия (`<x-guest-quick-actions />`)

---

### 3. **Поиск пользователей** (`/users`) - `users/search.blade.php`

#### Для авторизованных пользователей:

**Правая колонка:**
- ✅ **"Наша статистика"** (кастомный блок, grid 2x2)
  - Всего снов
  - Отчетов
  - Пользователей
  - Снов/отчет
- Друзья онлайн (если есть)
- Сонник дня
- Популярные теги (если есть)

#### Для неавторизованных пользователей:

**Левая колонка:**
- Приветственная карточка
- ✅ **"Статистика проекта"** (`<x-project-statistics variant="list" />`)

---

### 4. **Просмотр статьи** (`/articles/{slug}`) - `articles/show.blade.php`

**Левая колонка (для авторизованных):**
- Приветственная карточка
- Карточка пользователя
- Быстрое меню (`<x-auth-sidebar-menu />`)

**Левая колонка (для неавторизованных):**
- Приветственная карточка
- ✅ **"Статистика проекта"** (`<x-project-statistics variant="list" />`)
- Быстрые действия (`<x-guest-quick-actions />`)

---

### 5. **Список статей** (`/articles`) - `articles/articles/index.blade.php`

**Левая колонка (для неавторизованных):**
- Приветственная карточка
- ✅ **"Статистика проекта"** (`<x-project-statistics variant="list" />`)
- Быстрые действия (`<x-guest-quick-actions />`)

---

### 6. **Список инструкций** (`/guide`) - `articles/guide/index.blade.php`

**Левая колонка (для неавторизованных):**
- Приветственная карточка
- ✅ **"Статистика проекта"** (`<x-project-statistics variant="list" />`)
- Быстрые действия (`<x-guest-quick-actions />`)

---

### 7. **Просмотр отчета** (`/reports/{id}`) - `reports/show.blade.php`

**Левая колонка (для неавторизованных):**
- Приветственная карточка
- ✅ **"Статистика проекта"** (`<x-project-statistics variant="list" />`)
- Быстрые действия (`<x-guest-quick-actions />`)

---

### 8. **Создание толкования** (`/tolkovanie-snov`) - `dream-analyzer/create.blade.php`

**Правая колонка:**

**Для неавторизованных:**
- ✅ **"Статистика проекта"** (`<x-project-statistics variant="grid" />`)

**Для авторизованных:**
- Приветственная карточка
- Карточка пользователя
- ✅ **"Статистика проекта"** (`<x-project-statistics variant="grid" />`)
- Быстрое меню (`<x-auth-sidebar-menu />`)

---

### 9. **Просмотр толкования** (`/tolkovanie-snov/{hash}`) - `dream-analyzer/show.blade.php`

**Правая колонка:**
- Похожие толкования (если есть)
- ✅ **"Статистика проекта"** (`<x-project-statistics variant="grid" />`)
- Быстрое меню (для авторизованных) / Быстрые действия (для неавторизованных)

---

### 10. **Просмотр анализа отчета** (`/reports/{id}/analysis`) - `reports/analysis.blade.php`

**Правая колонка:**
- Похожие толкования (если есть)
- ✅ **"Статистика проекта"** (`<x-project-statistics variant="grid" />`)
- Быстрое меню (для авторизованных) / Быстрые действия (для неавторизованных)

---

## 📊 Сводная таблица

| Страница | Левая колонка | Правая колонка | Примечание |
|----------|---------------|----------------|------------|
| **Главная** (`/`) | - | "Наша статистика" (grid) + "Статистика проекта" (list) | Для авторизованных: обе в правой колонке |
| **Главная** (`/`) | "Статистика проекта" (list) | "Статистика проекта" (list) | Для неавторизованных: в обеих колонках |
| **Лента активности** (`/activity`) | - | "Наша статистика" (grid) | Для авторизованных |
| **Лента активности** (`/activity`) | "Статистика проекта" (list) | - | Для неавторизованных |
| **Поиск пользователей** (`/users`) | - | "Наша статистика" (grid) | Для авторизованных |
| **Поиск пользователей** (`/users`) | "Статистика проекта" (list) | - | Для неавторизованных |
| **Статья** (`/articles/{slug}`) | "Статистика проекта" (list) | - | Для неавторизованных |
| **Список статей** (`/articles`) | "Статистика проекта" (list) | - | Для неавторизованных |
| **Список инструкций** (`/guide`) | "Статистика проекта" (list) | - | Для неавторизованных |
| **Отчет** (`/reports/{id}`) | "Статистика проекта" (list) | - | Для неавторизованных |
| **Создание толкования** (`/tolkovanie-snov`) | - | "Статистика проекта" (grid) | Для всех |
| **Просмотр толкования** (`/tolkovanie-snov/{hash}`) | - | "Статистика проекта" (grid) | Для всех |
| **Анализ отчета** (`/reports/{id}/analysis`) | - | "Статистика проекта" (grid) | Для всех |

---

## 🔍 Детали блоков

### Блок "Наша статистика" (кастомный)

**Используется на:**
- Главная (`/`) - правая колонка (авторизованные)
- Лента активности (`/activity`) - правая колонка (авторизованные)
- Поиск пользователей (`/users`) - правая колонка (авторизованные)

**Содержимое:**
```php
- Всего снов: $globalStats['dreams']
- Отчетов: $globalStats['reports']
- Пользователей: $globalStats['users']
- Снов/отчет: $globalStats['avg_dreams_per_report']
```

**Формат:** Grid 2x2 с карточками

---

### Компонент "Статистика проекта" (`<x-project-statistics>`)

**Варианты:**
- `variant="list"` - вертикальный список (используется чаще)
- `variant="grid"` - сетка 2x2 (используется на страницах анализа)

**Содержимое:**
```php
- Пользователей: $stats['users']
- Отчетов: $stats['reports']
- Снов: $stats['dreams']
- Толкований: $stats['interpretations']
```

**Используется на:**
- Главная (`/`) - правая колонка (авторизованные), левая/правая (неавторизованные)
- Лента активности (`/activity`) - левая колонка (неавторизованные)
- Поиск пользователей (`/users`) - левая колонка (неавторизованные)
- Статья (`/articles/{slug}`) - левая колонка (неавторизованные)
- Список статей (`/articles`) - левая колонка (неавторизованные)
- Список инструкций (`/guide`) - левая колонка (неавторизованные)
- Отчет (`/reports/{id}`) - левая колонка (неавторизованные)
- Создание толкования (`/tolkovanie-snov`) - правая колонка (все)
- Просмотр толкования (`/tolkovanie-snov/{hash}`) - правая колонка (все)
- Анализ отчета (`/reports/{id}/analysis`) - правая колонка (все)

---

## ⚠️ Наблюдения

1. **Дублирование на главной странице:**
   - Для авторизованных: "Наша статистика" и "Статистика проекта" находятся в одной правой колонке
   - Для неавторизованных: "Статистика проекта" дублируется в левой и правой колонках

2. **Разные форматы:**
   - "Наша статистика" - всегда grid 2x2
   - "Статистика проекта" - list или grid в зависимости от страницы

3. **Разное содержимое:**
   - "Наша статистика" включает "Снов/отчет"
   - "Статистика проекта" включает "Толкований"

4. **Разные переменные:**
   - "Наша статистика" использует `$globalStats`
   - "Статистика проекта" использует `$stats` или `$globalStats`

---

## 📝 Рекомендации

1. **Унификация:**
   - Рассмотреть объединение блоков "Наша статистика" и "Статистика проекта"
   - Или четко разделить их назначение

2. **Устранение дублирования:**
   - На главной странице для неавторизованных убрать дублирование "Статистика проекта"
   - Для авторизованных рассмотреть объединение двух блоков статистики

3. **Консистентность:**
   - Использовать одинаковый формат (list или grid) на похожих страницах
   - Унифицировать названия и содержимое

---

**Дата создания:** 2026-01-24
