# 🚀 Команды для деплоя исправления CLS

**Дата:** 2026-01-24  
**Коммит:** Исправление CLS 0.233: добавлено резервирование места для главного контейнера и условных блоков

---

## 📋 Последовательность команд для деплоя

### 1. Подключение к серверу

```bash
ssh adminfeg@adminfeg.beget.tech
```

---

### 2. Переход в директорию проекта

```bash
cd ~/snovidec.ru/public_html
```

---

### 3. Получение последних изменений из репозитория

```bash
git pull origin main
```

---

### 4. Установка зависимостей Composer (production)

```bash
php8.3 /home/a/adminfeg/.local/bin/composer install --no-dev --optimize-autoloader
```

---

### 5. Установка зависимостей npm и сборка фронтенда

**⚠️ ВАЖНО:** Обязательно пересобрать фронтенд, так как изменён CSS!

```bash
cd ~/snovidec.ru/public_html
npm install
npm run build
```

**Проверка:** После сборки должен появиться новый файл `public/build/assets/app-DxuLtNa1.css`

---

### 6. Очистка кеша Laravel

```bash
php8.3 artisan cache:clear
php8.3 artisan config:clear
php8.3 artisan route:clear
php8.3 artisan view:clear
```

---

### 7. Пересборка кеша (опционально, для production)

```bash
php8.3 artisan config:cache
php8.3 artisan route:cache
php8.3 artisan view:cache
php8.3 artisan optimize
```

---

## ✅ Проверка после деплоя

### 1. Проверить PageSpeed Insights

Откройте: https://pagespeed.web.dev/

Проверьте главную страницу: `https://snovidec.ru/`

**Ожидаемые улучшения:**
- **CLS:** должен быть < 0.1 (было 0.233) ✅
- **Performance Score:** должен быть 90-92 (было 88) ✅

### 2. Проверить работу сайта

- Откройте главную страницу
- Проверьте, что все блоки отображаются корректно
- Убедитесь, что нет ошибок в консоли браузера (F12)

### 3. Проверить Lighthouse локально

- F12 → Маяк (Lighthouse)
- Запустите проверку Performance
- Проверьте CLS - должен быть < 0.1 ✅

### 4. Проверить HTML (опционально)

В браузере (Ctrl+U) или через curl:
```bash
curl -s https://snovidec.ru/ | grep -i "min-height" | head -5
```

Должны увидеть:
- `style="min-height: 800px;"` на главном контейнере
- `style="min-height: 180px;"` на приветственных карточках
- `style="min-height: 150px;"` на заголовках ленты

---

## 📝 Что было изменено

### Исправления CLS (Cumulative Layout Shift):

1. **Главный контейнер:**
   - Добавлен `style="min-height: 800px;"` на `<div class="w-full max-w-[1600px]...">`
   - Это был проблемный элемент с CLS 0.233

2. **Сетка main-grid:**
   - Добавлен `min-height: 600px;` в CSS для `.main-grid`

3. **Приветственные карточки:**
   - Добавлен `style="min-height: 180px;"` на обе карточки (авторизованные и неавторизованные)

4. **Заголовки ленты:**
   - Добавлен `style="min-height: 150px;"` на оба заголовка

### Файлы:
- `resources/css/app.css` - добавлен min-height для .main-grid
- `resources/views/welcome.blade.php` - добавлены min-height на контейнеры и карточки
- `public/build/assets/app-DxuLtNa1.css` - пересобран CSS (новый файл)

---

## 🔧 Если что-то пошло не так

### Откат изменений:

```bash
cd ~/snovidec.ru/public_html
git log --oneline -5  # Посмотреть последние коммиты
git reset --hard HEAD~1  # Откатить последний коммит (ОСТОРОЖНО!)
git pull origin main  # Получить актуальную версию
```

### Очистка кеша браузера:

После деплоя может потребоваться очистить кеш браузера или использовать режим инкогнито для проверки.

---

## 📊 Ожидаемые результаты

### До исправления:
- **CLS:** 0.233 🟠
- **Performance Score:** 88/100 🟠
- **Проблемный элемент:** `<div class="w-full max-w-[1600px]...">`

### После исправления:
- **CLS:** < 0.1 🟢 (ожидается значительное улучшение)
- **Performance Score:** 90-92/100 🟢
- **Причина:** Браузер теперь резервирует место заранее, предотвращая layout shift

---

## 🎯 Критически важно

1. **Обязательно пересобрать фронтенд** (`npm run build`) - без этого CSS изменения не применятся!
2. **Очистить кеш Laravel** - чтобы Blade шаблоны перекомпилировались
3. **Проверить через PageSpeed Insights** - убедиться, что CLS снизился

---

**Статус:** ✅ Готово к деплою
