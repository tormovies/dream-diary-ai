# 📋 Инструкции по проверке SEO: Alt теги, скорость, мобильная адаптивность

**Дата:** 2026-01-24

---

## 1️⃣ Проверка Alt тегов

### Автоматическая проверка (Artisan команда)

**Команда:**
```bash
cd C:\Users\torle\snovidec
php artisan seo:check-alt-tags
```

**Что проверяет:**
- ✅ Все Blade шаблоны на наличие `<img>` без alt
- ✅ Использование компонента `lazy-image` без alt параметра
- ✅ Контент статей в БД на наличие `<img>` без alt

**Результат:**
- Отчет в консоли
- Файл `alt_tags_check.txt` с детальным отчетом

**Если найдены проблемы:**
1. Откройте файл `alt_tags_check.txt`
2. Исправьте найденные проблемы
3. Запустите проверку снова

---

### Ручная проверка через браузер

**Chrome DevTools:**
1. Откройте любую страницу: `http://127.0.0.1:3000`
2. Нажмите F12
3. Перейдите на вкладку **Elements**
4. Нажмите Ctrl+F и введите: `img`
5. Проверьте каждый `<img>` тег:
   - Должен быть атрибут `alt`
   - Значение alt должно быть осмысленным (или пустым `alt=""` для декоративных)

**Lighthouse (Accessibility):**
1. Откройте страницу: `http://127.0.0.1:3000`
2. F12 → **Lighthouse**
3. Выберите категорию: **Accessibility**
4. Нажмите **Analyze page load**
5. Проверьте раздел "Images" - не должно быть предупреждений об отсутствии alt

---

## 2️⃣ Проверка скорости через PageSpeed Insights

### Онлайн проверка (рекомендуется)

**URL:** https://pagespeed.web.dev/

**Шаги:**
1. Откройте https://pagespeed.web.dev/
2. Вставьте URL страницы (например, `https://snovidec.ru/`)
3. Выберите **Mobile** или **Desktop**
4. Нажмите **Analyze**

**Что проверить:**
- ✅ Главная: `https://snovidec.ru/`
- ✅ Статья: `https://snovidec.ru/articles/{slug}`
- ✅ Инструкция: `https://snovidec.ru/guide/{slug}`
- ✅ Толкование: `https://snovidec.ru/tolkovanie-snov/{hash}`

**Что записать:**
- Performance Score (0-100)
- Core Web Vitals:
  - **LCP** (Largest Contentful Paint) - должен быть < 2.5s
  - **FID/INP** (First Input Delay / Interaction to Next Paint) - должен быть < 100ms
  - **CLS** (Cumulative Layout Shift) - должен быть < 0.1
- Основные проблемы и рекомендации

**Создать файл:** `pagespeed_results.md` с результатами

---

### Локальная проверка через Lighthouse

**Chrome DevTools:**
1. Откройте `http://127.0.0.1:3000` в Chrome
2. Нажмите F12
3. Перейдите на вкладку **Lighthouse**
4. Выберите категории:
   - ✅ Performance
   - ✅ Accessibility
   - ✅ Best Practices
   - ✅ SEO
5. Выберите устройство: **Mobile** или **Desktop**
6. Нажмите **Analyze page load**
7. Дождитесь завершения анализа
8. Сохраните отчет (кнопка "Export" или скриншот)

**Проверить страницы:**
- `/` - Главная
- `/articles/{slug}` - Статья
- `/guide/{slug}` - Инструкция
- `/tolkovanie-snov/{hash}` - Толкование
- `/users/{id}` - Профиль

**Что обратить внимание:**
- Performance Score (цель: > 90)
- Core Web Vitals (все должны быть зелеными)
- Основные проблемы:
  - Неоптимизированные изображения
  - Неиспользуемый CSS/JS
  - Медленные запросы к БД
  - Отсутствие кеширования

---

### Командная строка (Lighthouse CLI)

**Установка:**
```bash
npm install -g lighthouse
```

**Проверка:**
```bash
lighthouse http://127.0.0.1:3000 --view --output html --output-path ./lighthouse-report.html
```

**Параметры:**
- `--view` - открыть отчет в браузере
- `--output html` - сохранить как HTML
- `--output-path` - путь для сохранения

---

## 3️⃣ Проверка мобильной адаптивности

### Онлайн проверка (Google Mobile-Friendly Test)

**URL:** https://search.google.com/test/mobile-friendly

**Шаги:**
1. Откройте https://search.google.com/test/mobile-friendly
2. Вставьте URL страницы (например, `https://snovidec.ru/`)
3. Нажмите **Test URL**
4. Дождитесь результата

**Что проверить:**
- ✅ Главная: `https://snovidec.ru/`
- ✅ Статья: `https://snovidec.ru/articles/{slug}`
- ✅ Инструкция: `https://snovidec.ru/guide/{slug}`
- ✅ Толкование: `https://snovidec.ru/tolkovanie-snov/{hash}`

**Что записать:**
- Статус: Mobile-friendly или нет
- Основные проблемы (если есть):
  - Размер шрифтов (минимум 16px)
  - Расстояние между кликабельными элементами (минимум 48px)
  - Горизонтальная прокрутка
  - Контент не помещается на экране

**Создать файл:** `mobile_friendly_results.md` с результатами

---

### Ручная проверка в браузере

**Chrome DevTools:**
1. Откройте страницу: `http://127.0.0.1:3000`
2. Нажмите F12
3. Нажмите **Toggle device toolbar** (Ctrl+Shift+M) или иконку устройства
4. Выберите устройство:
   - iPhone 12 Pro
   - iPhone SE
   - iPad
   - Samsung Galaxy S20
   - Или настройте кастомный размер

**Что проверить:**
- ✅ Все элементы видны и доступны
- ✅ Шрифты читаемы (минимум 16px)
- ✅ Кнопки и ссылки кликабельны (минимум 48x48px)
- ✅ Нет горизонтальной прокрутки
- ✅ Формы удобны для заполнения
- ✅ Навигация работает корректно
- ✅ Модальные окна отображаются правильно

**Проверить страницы:**
- `/` - Главная
- `/articles/{slug}` - Статья
- `/guide/{slug}` - Инструкция
- `/tolkovanie-snov/{hash}` - Толкование
- `/reports/create` - Создание отчета
- `/dashboard` - Дашборд
- `/users/{id}` - Профиль

---

### Firefox Responsive Design Mode

**Шаги:**
1. Откройте страницу в Firefox
2. Нажмите F12
3. Нажмите **Responsive Design Mode** (Ctrl+Shift+M)
4. Выберите устройство или настройте кастомный размер
5. Проверьте отображение

---

## 📊 Шаблон отчета

### Alt теги:
```
✅ Компонент lazy-image: Поддерживает alt
✅ Найдено <img> без alt: X мест
✅ Статьи с <img> без alt: Y статей
✅ Процент с alt: XX%
```

### Скорость (PageSpeed Insights):
```
Главная:
- Performance Score: XX/100
- LCP: X.Xs
- FID/INP: XXms
- CLS: 0.XXX

Статья:
- Performance Score: XX/100
- LCP: X.Xs
- FID/INP: XXms
- CLS: 0.XXX

Основные проблемы:
1. ...
2. ...
```

### Мобильная адаптивность:
```
Mobile-Friendly Test:
- Главная: ✅ Mobile-friendly
- Статья: ✅ Mobile-friendly
- Инструкция: ✅ Mobile-friendly

Проблемы:
- (если есть)
```

---

## 🚀 Быстрый старт

### 1. Проверка Alt тегов (5 минут):
```bash
cd C:\Users\torle\snovidec
php artisan seo:check-alt-tags
```

### 2. Проверка скорости (10 минут):
1. Откройте https://pagespeed.web.dev/
2. Проверьте главную страницу
3. Запишите результаты

### 3. Проверка мобильной адаптивности (5 минут):
1. Откройте https://search.google.com/test/mobile-friendly
2. Проверьте главную страницу
3. Запишите результаты

---

## 📝 Чеклист

- [ ] Запустить `php artisan seo:check-alt-tags`
- [ ] Проверить результаты alt тегов
- [ ] Исправить найденные проблемы с alt тегами
- [ ] Проверить главную через PageSpeed Insights
- [ ] Проверить статью через PageSpeed Insights
- [ ] Записать результаты скорости
- [ ] Проверить главную через Mobile-Friendly Test
- [ ] Проверить статью через Mobile-Friendly Test
- [ ] Записать результаты мобильной адаптивности
- [ ] Проверить вручную через DevTools (разные устройства)
- [ ] Создать итоговый отчет

---

**Готово!** Начинаем проверку.
