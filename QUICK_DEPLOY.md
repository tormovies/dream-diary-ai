# 🚀 Быстрый деплой на продакшен

## ✅ Изменения в этом релизе:

### Основные улучшения:
1. ✅ **HTML-форматирование от DeepSeek API**
   - Автоматическая санитизация HTML (h2, h3, p, ul, li, strong, em)
   - Корректное отображение списков с автоматическим оборачиванием в `<ul>`
   - Удаление дублирующихся заголовков

2. ✅ **Поддержка промптов для каждой традиции**
   - Добавлено поле `single_tradition_prompt` в `config/traditions.php`
   - Улучшенное объединение промптов для нескольких традиций (", а также" и "\n\n")

3. ✅ **Увеличенные таймауты**
   - HTTP запросы: 600 секунд (10 минут)
   - PHP execution: 660 секунд (11 минут)

4. ✅ **Статистика обработки (для админов)**
   - Время обработки
   - Количество токенов (prompt/completion/total)

5. ✅ **Улучшенное отображение серий снов**
   - Ключевые символы в виде плитки (3 колонки)
   - Блок "Анализ в традиции" для серий
   - Корректная обработка `meaning` и `<meaning>` ключей

6. ✅ **Улучшения UI**
   - Традиции отображаются 3 в ряд (вместо 2)
   - Исправлены наложения текста в плитках символов

---

## 🔧 Команды для деплоя на сервере:

### Вариант 1: Пошагово (рекомендуется)

```bash
# 1. Подключение к серверу
ssh user@your-server.com

# 2. Переход в директорию проекта
cd ~/snovidec.ru/laravel

# 3. Обновление кода из GitHub
git pull origin main

# 4. Установка зависимостей PHP
composer install --no-dev --optimize-autoloader

# 5. Установка зависимостей Node.js и сборка
npm install
npm run build

# 6. Миграции (если есть новые)
php artisan migrate --force

# 7. Очистка кешей
php artisan config:clear
php artisan route:clear
php artisan view:clear
php artisan cache:clear

# 8. Кеширование для продакшена
php artisan config:cache
php artisan route:cache
php artisan view:cache

# 9. Права доступа (если нужно)
sudo chown -R www-data:www-data storage bootstrap/cache
sudo chmod -R 775 storage bootstrap/cache

# 10. Проверка логов
tail -f storage/logs/laravel.log
```

---

### Вариант 2: Всё одной командой

```bash
cd ~/snovidec.ru/laravel && \
git pull origin main && \
composer install --no-dev --optimize-autoloader && \
npm install && npm run build && \
php artisan migrate --force && \
php artisan config:clear && \
php artisan route:clear && \
php artisan view:clear && \
php artisan cache:clear && \
php artisan config:cache && \
php artisan route:cache && \
php artisan view:cache && \
sudo chown -R www-data:www-data storage bootstrap/cache && \
sudo chmod -R 775 storage bootstrap/cache && \
echo "✅ Деплой завершён!"
```

---

## ⚠️ Перед деплоем проверьте на сервере:

```bash
# Проверка .env
cat .env | grep -E "APP_ENV|APP_DEBUG|DEEPSEEK_API_KEY"

# Должно быть:
# APP_ENV=production
# APP_DEBUG=false
# DEEPSEEK_API_KEY=sk-...
```

---

## ✅ После деплоя проверьте:

1. **Основные страницы:**
   - [ ] Главная страница открывается
   - [ ] Авторизация работает
   - [ ] Админ-панель доступна

2. **Анализ снов:**
   - [ ] `/tolkovanie-snov/` - форма создания
   - [ ] Выбор традиций (3 в ряд)
   - [ ] Отправка анализа
   - [ ] Отображение результатов с HTML-форматированием
   - [ ] Плитки символов (3 колонки)

3. **Анализ отчётов:**
   - [ ] `/reports/{id}/analysis` - работает
   - [ ] Для админа видна статистика (время, токены)

4. **Проверка логов:**
   ```bash
   tail -n 50 storage/logs/laravel.log
   ```

---

## 🔄 Если нужен откат:

```bash
# Посмотреть последние коммиты
git log --oneline -5

# Откатить на предыдущий коммит
git reset --hard 779ae85

# Повторить очистку кешей
php artisan config:clear && php artisan route:clear && php artisan view:clear
```

---

## 📝 Технические детали:

### Изменённые файлы:
- `app/Http/Controllers/DreamAnalyzerController.php` - увеличены таймауты
- `app/Http/Controllers/ReportController.php` - увеличены таймауты, статистика
- `app/Services/DeepSeekService.php` - таймаут 600 сек, улучшенные промпты
- `app/Helpers/HtmlHelper.php` - санитизация HTML, оборачивание списков
- `app/Helpers/SeoHelper.php` - очистка HTML из meta-тегов
- `app/Helpers/TraditionHelper.php` - метод `singleTraditionPrompt()`
- `config/traditions.php` - добавлено поле `single_tradition_prompt`
- Все Blade партиалы анализа - HTML рендеринг, улучшенная вёрстка
- `resources/views/dream-analyzer/create.blade.php` - 3 колонки традиций
- `resources/views/reports/analysis.blade.php` - статистика для админа

### База данных:
**Новых миграций нет** - используется существующая структура.

---

## 🆘 Контакты поддержки:

Если что-то пошло не так:
1. Проверьте логи: `storage/logs/laravel.log`
2. Проверьте права: `ls -la storage/`
3. Проверьте .env: `cat .env`
4. Откатите изменения (см. выше)

---

**Дата деплоя:** 2026-01-04  
**Версия:** 3f36027  
**Ветка:** main

