# 🚀 Команды для деплоя агрессивного исправления CLS

**Дата:** 2026-01-24  
**Коммит:** Агрессивное исправление CLS: увеличен min-height до 1200px, добавлен contain: layout

---

## ⚠️ ВАЖНО: Проверка предыдущего деплоя

**Перед началом проверьте:**

1. **Были ли предыдущие изменения задеплоены?**
   ```bash
   cd ~/snovidec.ru/public_html
   git log --oneline -5
   ```
   Должен быть коммит: "Исправление CLS 0.233..."

2. **Был ли пересобран фронтенд?**
   ```bash
   ls -la public/build/assets/app-*.css | tail -3
   ```
   Должен быть файл: `app-DxuLtNa1.css` или `app-COn3ZSwi.css`

3. **Проверьте HTML на сервере:**
   ```bash
   curl -s https://snovidec.ru/ | grep -i "min-height" | head -3
   ```
   Должны увидеть `min-height` в HTML

---

## 📋 Последовательность команд для деплоя

### 1. Подключение к серверу

```bash
ssh adminfeg@adminfeg.beget.tech
```

---

### 2. Переход в директорию проекта

```bash
cd ~/snovidec.ru/public_html
```

---

### 3. Получение последних изменений из репозитория

```bash
git pull origin main
```

---

### 4. Установка зависимостей Composer (production)

```bash
php8.3 /home/a/adminfeg/.local/bin/composer install --no-dev --optimize-autoloader
```

---

### 5. Установка зависимостей npm и сборка фронтенда

**⚠️ КРИТИЧЕСКИ ВАЖНО:** Обязательно пересобрать фронтенд!

```bash
cd ~/snovidec.ru/public_html
npm install
npm run build
```

**Проверка после сборки:**
```bash
ls -la public/build/assets/app-*.css | tail -1
```

Должен появиться новый файл: `app-COn3ZSwi.css` (или новее)

---

### 6. Очистка кеша Laravel

```bash
php8.3 artisan cache:clear
php8.3 artisan config:clear
php8.3 artisan route:clear
php8.3 artisan view:clear
```

---

### 7. Пересборка кеша (опционально, для production)

```bash
php8.3 artisan config:cache
php8.3 artisan route:cache
php8.3 artisan view:cache
php8.3 artisan optimize
```

---

## ✅ Проверка после деплоя

### 1. Проверить HTML на сервере

```bash
curl -s https://snovidec.ru/ | grep -i "min-height.*1200px" | head -1
```

Должны увидеть: `style="min-height: 1200px; contain: layout;"`

### 2. Проверить PageSpeed Insights

Откройте: https://pagespeed.web.dev/

Проверьте главную страницу: `https://snovidec.ru/`

**Ожидаемые улучшения:**
- **CLS:** должен быть < 0.1 (было 0.235) ✅
- **Performance Score:** должен быть 90-92 (было 88) ✅

### 3. Проверить работу сайта

- Откройте главную страницу
- Проверьте, что все блоки отображаются корректно
- Убедитесь, что нет ошибок в консоли браузера (F12)

### 4. Проверить Lighthouse локально

- F12 → Маяк (Lighthouse)
- Запустите проверку Performance
- Проверьте CLS - должен быть < 0.1 ✅

---

## 📝 Что было изменено

### Агрессивные исправления CLS:

1. **Главный контейнер:**
   - **Было:** `min-height: 800px;`
   - **Стало:** `min-height: 1200px; contain: layout;`
   - Это был проблемный элемент с CLS 0.235

2. **Сетка main-grid:**
   - **В CSS:** `min-height: 800px; contain: layout;`
   - **Inline:** `min-height: 1000px;` на обеих версиях (авторизованные и неавторизованные)

3. **Приветственные карточки:**
   - Остались: `min-height: 180px;`

4. **Заголовки ленты:**
   - Остались: `min-height: 150px;`

### Файлы:
- `resources/css/app.css` - увеличен min-height для .main-grid, добавлен contain: layout
- `resources/views/welcome.blade.php` - увеличен min-height на контейнерах
- `public/build/assets/app-COn3ZSwi.css` - пересобран CSS (новый файл)

---

## 🔧 Если CLS всё ещё высокий

### Возможные причины:

1. **Изменения не задеплоены** - проверьте git log
2. **Фронтенд не пересобран** - проверьте наличие app-COn3ZSwi.css
3. **Кеш браузера** - используйте режим инкогнито
4. **Другие элементы вызывают shift** - проверьте через Performance Panel в Chrome

### Дополнительная диагностика:

```bash
# Проверить, что изменения в HTML
curl -s https://snovidec.ru/ | grep -o 'min-height:[^;]*' | head -5

# Проверить, что CSS загружается
curl -s https://snovidec.ru/build/assets/app-COn3ZSwi.css | grep -i "min-height" | head -3
```

---

## 📊 Ожидаемые результаты

### До исправления:
- **CLS:** 0.235 🟠
- **Performance Score:** 88/100 🟠

### После исправления:
- **CLS:** < 0.1 🟢 (ожидается значительное улучшение)
- **Performance Score:** 90-92/100 🟢
- **Причина:** Более агрессивное резервирование места + `contain: layout` предотвращает layout shift

---

## 🎯 Критически важно

1. **Обязательно пересобрать фронтенд** (`npm run build`) - без этого CSS изменения не применятся!
2. **Очистить кеш Laravel** - чтобы Blade шаблоны перекомпилировались
3. **Проверить через PageSpeed Insights** - убедиться, что CLS снизился
4. **Использовать режим инкогнито** для проверки - чтобы исключить кеш браузера

---

**Статус:** ✅ Готово к деплою
