# 🔧 Исправление CLS (Cumulative Layout Shift) - 0.233

**Проблема:** Главный контейнер `<div class="w-full max-w-[1600px] mx-auto px-4 sm:px-6 lg:px-8 py-6">` вызывает CLS 0.233

**Причина:** Контейнер и условные блоки (@auth/@else) не резервируют место заранее, что вызывает layout shift при загрузке контента.

---

## ✅ Применённые исправления

### 1. Главный контейнер
- **Файл:** `resources/views/welcome.blade.php`
- **Изменение:** Добавлен `style="min-height: 800px;"` на главный контейнер
- **Строка:** 5

### 2. Сетка main-grid
- **Файл:** `resources/css/app.css`
- **Изменение:** Добавлен `min-height: 600px;` для `.main-grid`
- **Строка:** 51

### 3. Приветственные карточки
- **Файл:** `resources/views/welcome.blade.php`
- **Изменение:** Добавлен `style="min-height: 180px;"` на обе приветственные карточки (для авторизованных и неавторизованных)
- **Строки:** 12, 278

### 4. Заголовки ленты
- **Файл:** `resources/views/welcome.blade.php`
- **Изменение:** Добавлен `style="min-height: 150px;"` на оба заголовка ленты (для авторизованных и неавторизованных)
- **Строки:** 34, 295

---

## 📊 Ожидаемый результат

### До исправления:
- **CLS:** 0.233 🟠
- **Элемент:** `<div class="w-full max-w-[1600px] mx-auto px-4 sm:px-6 lg:px-8 py-6">`

### После исправления:
- **CLS:** < 0.1 🟢 (ожидается значительное улучшение)
- **Причина:** Браузер теперь знает заранее размеры контейнеров и резервирует место

---

## 🔍 Что было сделано

1. ✅ Добавлен `min-height: 800px` на главный контейнер
2. ✅ Добавлен `min-height: 600px` на `.main-grid`
3. ✅ Добавлен `min-height: 180px` на приветственные карточки
4. ✅ Добавлен `min-height: 150px` на заголовки ленты
5. ✅ Фронтенд пересобран (`npm run build`)

---

## 🚀 Следующие шаги

1. **Закоммитить и запушить изменения**
2. **Деплой на сервер**
3. **Пересобрать фронтенд на сервере:** `npm run build`
4. **Проверить через PageSpeed Insights:**
   - Открыть https://pagespeed.web.dev/
   - Проверить главную: `https://snovidec.ru/`
   - Ожидается CLS < 0.1

---

## 📝 Изменённые файлы

1. ✅ `resources/css/app.css` - добавлен min-height для .main-grid
2. ✅ `resources/views/welcome.blade.php` - добавлены min-height на контейнеры и карточки
3. ✅ `public/build/assets/app-DxuLtNa1.css` - пересобран CSS

---

**Статус:** ✅ Исправления применены, готово к тестированию
