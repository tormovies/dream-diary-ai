# 🔧 Исправление деплоя на сервере

**Проблема:** На сервере не найдено `min-height: 1200px` в HTML

**Причина:** Вы находитесь в `~/snovidec.ru/laravel`, а не в `public_html`

---

## 📋 Команды для исправления

### 1. Проверить структуру проекта

```bash
ls -la ~/snovidec.ru/
```

Определите, где находится проект:
- `~/snovidec.ru/public_html` - если Laravel в корне
- `~/snovidec.ru/laravel` - если Laravel в отдельной папке

---

### 2. Перейти в правильную директорию

**Вариант A: Если Laravel в корне**
```bash
cd ~/snovidec.ru/public_html
```

**Вариант B: Если Laravel в отдельной папке**
```bash
cd ~/snovidec.ru/laravel
```

---

### 3. Проверить последние коммиты

```bash
git log --oneline -5
```

**Должен быть коммит:** "Агрессивное исправление CLS: увеличен min-height до 1200px..."

Если его нет → нужно сделать `git pull`

---

### 4. Получить последние изменения

```bash
git pull origin main
```

---

### 5. Проверить файл welcome.blade.php

```bash
grep -n "min-height.*1200px" resources/views/welcome.blade.php
```

**Должны увидеть:** строку с `min-height: 1200px`

Если не видно → файл не обновлён

---

### 6. Пересобрать фронтенд (КРИТИЧЕСКИ ВАЖНО!)

```bash
npm install
npm run build
```

**Проверка после сборки:**
```bash
ls -la public/build/assets/app-*.css | tail -1
```

Должен быть файл: `app-COn3ZSwi.css` (или новее)

---

### 7. Очистить кеш Laravel

```bash
php8.3 artisan view:clear
php8.3 artisan cache:clear
php8.3 artisan config:clear
php8.3 artisan route:clear
```

---

### 8. Пересобрать кеш (опционально)

```bash
php8.3 artisan view:cache
php8.3 artisan config:cache
php8.3 artisan route:cache
```

---

### 9. Проверить HTML после всех действий

```bash
curl -s https://snovidec.ru/ | grep -i "min-height.*1200px"
```

**Должны увидеть:** `style="min-height: 1200px; contain: layout;"`

---

### 10. Если всё ещё не видно - проверьте путь

```bash
# Найти где находится welcome.blade.php
find ~/snovidec.ru -name "welcome.blade.php" 2>/dev/null

# Проверить какой файл используется
grep -r "min-height.*1200px" ~/snovidec.ru/resources/views/ 2>/dev/null
```

---

## 🔍 Диагностика

### Проверить текущую директорию:
```bash
pwd
```

### Проверить структуру:
```bash
ls -la
```

### Проверить git статус:
```bash
git status
```

### Проверить последний коммит:
```bash
git log -1 --oneline
```

---

## ✅ Итоговая проверка

После всех действий выполните:

```bash
curl -s https://snovidec.ru/ | grep -o 'min-height:[^;]*' | head -5
```

**Должны увидеть:**
- `min-height: 1200px`
- `min-height: 1000px`
- `min-height: 180px`
- `min-height: 150px`

---

## 🎯 Быстрая команда (если Laravel в отдельной папке)

```bash
cd ~/snovidec.ru/laravel && \
git pull origin main && \
npm run build && \
php8.3 artisan view:clear && \
php8.3 artisan cache:clear && \
curl -s https://snovidec.ru/ | grep -i "min-height.*1200px"
```

---

**Статус:** ⚠️ Требуется выполнить команды на сервере
