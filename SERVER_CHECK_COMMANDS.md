# 🔍 Проверка и исправление на сервере

**Проблема:** На сервере не найдено `min-height: 1200px` в HTML

**Причина:** Изменения не применены или не задеплоены

---

## 📋 Команды для проверки и исправления

### 1. Проверить текущую директорию

```bash
pwd
```

**Должно быть:** `~/snovidec.ru/public_html` или `~/snovidec.ru/laravel/public`

Если вы в `~/snovidec.ru/laravel`, перейдите в правильную директорию:
```bash
cd ~/snovidec.ru/public_html
# или
cd ~/snovidec.ru/laravel/public
```

---

### 2. Проверить последние коммиты

```bash
git log --oneline -5
```

**Должен быть коммит:** "Агрессивное исправление CLS: увеличен min-height до 1200px..."

Если его нет → нужно сделать `git pull`

---

### 3. Получить последние изменения

```bash
git pull origin main
```

---

### 4. Проверить файл welcome.blade.php

```bash
grep -n "min-height.*1200px" resources/views/welcome.blade.php
```

**Должны увидеть:** строку с `min-height: 1200px`

Если не видно → файл не обновлён, нужно сделать `git pull` ещё раз

---

### 5. Пересобрать фронтенд (ОБЯЗАТЕЛЬНО!)

```bash
npm install
npm run build
```

**Проверка после сборки:**
```bash
ls -la public/build/assets/app-*.css | tail -1
```

Должен быть файл: `app-COn3ZSwi.css` (или новее)

---

### 6. Очистить кеш Laravel

```bash
php8.3 artisan view:clear
php8.3 artisan cache:clear
php8.3 artisan config:clear
```

---

### 7. Проверить HTML после очистки кеша

```bash
curl -s https://snovidec.ru/ | grep -i "min-height.*1200px"
```

**Должны увидеть:** `style="min-height: 1200px; contain: layout;"`

---

### 8. Если всё ещё не видно

Проверьте правильный путь к проекту:

```bash
# Найти где находится welcome.blade.php
find ~ -name "welcome.blade.php" -path "*/snovidec*" 2>/dev/null

# Проверить структуру проекта
ls -la ~/snovidec.ru/
```

Возможно, структура проекта другая (например, Laravel в отдельной папке).

---

## 🔧 Альтернативный вариант

Если структура проекта другая, проверьте:

```bash
# Если Laravel в отдельной папке
cd ~/snovidec.ru/laravel
git pull origin main
npm run build
php8.3 artisan view:clear
php8.3 artisan cache:clear
```

---

## ✅ Итоговая проверка

После всех действий:

```bash
curl -s https://snovidec.ru/ | grep -o 'min-height:[^;]*' | head -5
```

**Должны увидеть:**
- `min-height: 1200px`
- `min-height: 1000px`
- `min-height: 180px`
- `min-height: 150px`

---

**Статус:** ⚠️ Требуется проверка и исправление на сервере
